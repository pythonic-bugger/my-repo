name: CI

on:
  repository_dispatch:
    types: on-demand-test
  push:

jobs:
  push-job:
    name: Somebody pushed me
    runs-on: ubuntu-latest
    steps:
      - name: Yell
        run: |
          echo "I was pushed"

      - name: Get current branch Git commit
        id: consumer_version
        run: echo "app_version=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Check stuff
        env:
          APP_VERSION: ${{ github.head_ref }}.${{ github.sha }}
        if: ${{ success() }}
        run: 
          echo "$APP_VERSION"
          echo "${GITHUB_SHA::7}"
